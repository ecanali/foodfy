<div class="item">
    <h4>Nome</h4>
    <div>
        <input
            type="text"
            name="name"
            value="{{ chef.name }}"
            required="required"
        >
    </div>
</div>

<div class="item">
    <h4>Avatar</h4>
    <div>
        <input
            type="url"
            name="avatar_url"
            placeholder="http://"
            value="{{ chef.avatar_url }}"
            required="required"
        >
    </div>
</div>
